---
description: Quick update and deploy to GitHub
---

# 快速部署到 GitHub

使用此 workflow 快速提交和推送更改到 GitHub。

## 使用方法

在對話中輸入：`/update`

## 流程步驟

### 1. 檢查狀態
```bash
git status
```

### 2. 添加所有更改
```bash
git add -A
```

### 3. 提交更改
```bash
git commit -m "你的提交訊息"
```
> **注意**: 提交訊息應該清楚描述本次更改內容

### 4. 推送到 GitHub
// turbo
```bash
git push origin main
```

### 5. 驗證
檢查 GitHub 上的更改是否成功推送

## 提交訊息格式建議

使用以下格式可以讓 commit 更清楚：

- `✨ feat: 新功能` - 新增功能
- `🐛 fix: 修復問題` - 錯誤修復
- `📝 docs: 文檔` - 文檔更新
- `💄 style: 樣式` - CSS/UI 更新
- `♻️ refactor: 重構` - 代碼重構
- `⚡️ perf: 性能` - 性能優化
- `✅ test: 測試` - 測試相關
- `🔧 chore: 雜項` - 其他更改

## 範例

### 新增功能
```bash
git commit -m "✨ feat: 新增用戶登入功能"
```

### 修復Bug
```bash
git commit -m "🐛 fix: 修復手機版佈局問題"
```

### 更新文檔
```bash
git commit -m "📝 docs: 更新 README 安裝說明"
```

## 注意事項

1. **確認更改**: 推送前確認 `git status` 顯示的更改都是你想要的
2. **有意義的訊息**: 提交訊息要清楚描述更改內容
3. **測試**: 重要更改應該先在本地測試
4. **版本號**: 重大更新記得更新 `VERSION` 檔案和 `CHANGELOG.md`

## 版本更新流程

如果是新版本發布：

### 1. 更新版本號
```bash
echo "2.3.0" > VERSION
```

### 2. 更新 CHANGELOG.md
在檔案頂部添加新版本說明

### 3. 創建 Release Notes（可選）
```bash
# 創建 RELEASE-NOTES-v2.3.md
```

### 4. 提交和推送
```bash
git add VERSION CHANGELOG.md RELEASE-NOTES-v2.3.md
git commit -m "🚀 Release v2.3: 版本描述"
git push origin main
```

### 5. 創建 Git Tag（可選）
```bash
git tag -a v2.3.0 -m "Release v2.3.0"
git push origin v2.3.0
```

---

**最後更新**: 2025-12-29
